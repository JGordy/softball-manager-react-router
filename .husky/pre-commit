# Run checks only on staged files using lint-staged
if ! npx lint-staged; then
    printf "\033[0;31mCommit aborted: lint-staged checks failed. Fix the reported issues (for example, run 'npx lint-staged') and stage the changes.\033[0m\n"
    exit 1
fi
